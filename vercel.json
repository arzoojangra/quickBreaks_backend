{
    "buildCommand": "cd frontend && npm install && npm start && cd ../backend && npm run dev",
    "outputDirectory": "client/dist",
    "framework": "react",
    "rewrites":[
        {
            "source": "/api/(.*)",
            "destination": "/backend/server.js"
        }
    ]
}